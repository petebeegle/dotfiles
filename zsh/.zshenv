export PATH="$PATH:$HOME/.rvm/bin"
export PATH="/$HOME/.rd/bin:$PATH"
export SDKMAN_DIR="$HOME/.sdkman"
export NVM_DIR="$HOME/.nvm"

# Ensure dotfiles location is available for .zshrc sourcing.
export DOTFILES_LOCATION="${DOTFILES_LOCATION:-$HOME/code/petebeegle/dotfiles}"
